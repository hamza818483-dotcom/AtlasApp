-- ============================================================
-- Timer Show/Hide টগল ফিচারের জন্য নতুন কলাম
-- Supabase Dashboard > SQL Editor এ গিয়ে এটা রান করতে হবে।
-- বিদ্যমান countdowns টেবিলের কোনো ডেটা মুছবে না — শুধু একটা
-- নতুন কলাম যোগ হবে, ডিফল্ট মান true (অর্থাৎ পুরনো সব টাইমার
-- আগের মতোই দেখাতে থাকবে, কিছু হঠাৎ লুকিয়ে যাবে না)।
-- ============================================================

ALTER TABLE countdowns
ADD COLUMN IF NOT EXISTS is_active BOOLEAN DEFAULT true;
