# 🖼️ Slideshow ফিচার — সেটআপ নির্দেশনা

## কী যুক্ত হয়েছে

- **Admin panel** (`admin.html`): "Special Card" আর "Serial" ট্যাবের মাঝে
  নতুন **"🖼️ Slideshow"** ট্যাব। এখানে একাধিক ছবি একসাথে সিলেক্ট করে
  আপলোড করা যায়, তালিকা দেখা যায়, ডিলিট করা যায়, এবং Serial Order মডাল
  দিয়ে ক্রম ঠিক করা যায়।
- **Home page** (`index.html`): header-এর ঠিক নিচে একটা auto-slideshow
  — right-to-left transition, প্রতি ২ সেকেন্ড পর পর পরের ছবিতে যায়,
  নিচে dot indicator থাকে।

## পুশ করার পর করণীয় (জরুরি)

### Supabase-এ টেবিল তৈরি করো

Supabase Dashboard → SQL Editor → `kakhacq/supabase-migration.sql`-এর
পুরো কনটেন্ট পেস্ট করে রান করো। এটা একটা নতুন `slideshow_images` টেবিল
তৈরি করবে (এবং সেই সাথে আগের `ka_kha_cq.parsed_content` কলামও, যদি
সেটা আগে করা না থাকে)। বিদ্যমান কোনো ডেটা মুছবে না।

টেবিল স্ট্রাকচার:

| কলাম | টাইপ | বিবরণ |
|---|---|---|
| `id` | UUID | প্রাইমারি কী, অটো জেনারেট |
| `image_url` | TEXT | ছবি (base64 data-URL হিসেবে সেইভ হয়, অন্য ছবি ফিচারের মতোই) |
| `caption` | TEXT | ঐচ্ছিক ক্যাপশন (বর্তমানে UI থেকে সেট করার অপশন নেই, ভবিষ্যতে যুক্ত করা যাবে) |
| `sort_order` | INTEGER | ক্রম — Serial Order মডাল দিয়ে পরিবর্তনযোগ্য |
| `created_at` | TIMESTAMPTZ | তৈরির সময় |

## নিরাপত্তা নোট ⚠️

`slideshow_images` টেবিলের RLS policy এই অ্যাপের অন্য সব পাবলিক টেবিলের
(special_cards, mentors ইত্যাদি) মতোই **public read+write** —
কারণ এই অ্যাপ admin verification ক্লায়েন্ট-সাইডেই করে, ডাটাবেস লেভেলে না।
এটা অ্যাপের সামগ্রিক প্যাটার্নের সাথে সামঞ্জস্যপূর্ণ, কিন্তু এর মানে
টেকনিক্যালি কেউ যদি Supabase URL ও anon key জোগাড় করে, সরাসরি API কল
করে এই টেবিলে লিখতে পারবে। ভবিষ্যতে security কড়া করতে চাইলে Supabase
Auth + সার্ভার-সাইড admin check যুক্ত করার কথা ভাবা যেতে পারে — এটা এই
ফিচারের স্কোপের বাইরে, তাই এখন touch করা হয়নি।

## ছবির সাইজ নিয়ে সতর্কতা

ছবিগুলো base64 হিসেবে সরাসরি ডাটাবেসে সেইভ হয় (ImgBB বা কোনো external
storage ব্যবহার হয় না, special_cards/mentors ফিচারের প্যাটার্ন অনুসরণ
করে)। বড় ছবি (যেমন ৫ MB+) আপলোড করলে row size বড় হয়ে যাবে এবং পেজ লোড
স্লো হতে পারে। সম্ভব হলে আপলোডের আগে ছবি কমপ্রেস/রিসাইজ করে নেওয়া ভালো
(সুপারিশ: ১২৮০px width-এর বেশি না)।
